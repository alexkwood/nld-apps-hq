extends ../layout

block mainContent
  h1= title
  p Welcome to #{title}

  - if (loggedIn)
      p Hello #{username}
      div
        a(href='/logout') Logout  

  - else
      a(href="/auth/facebook") login with facebook

