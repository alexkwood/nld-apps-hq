//- @todo this is mostly identical to your-lists.jade, consolidate

h2 Visited Lists
div#visited-lists
  - if (guestLists && guestLists.length)
    ol
      - each list in guestLists
        li
          a(href='#{listsBase}/list/#{list._id}')
            - if (typeof currentList != "undefined" && currentList._id && currentList._id.toString() == list._id.toString())
              strong= list.title
            - else
              span= list.title
              | 
              span.item-count (#{list.countItems()} items)

  - else
    div You haven't visited anyone else's lists yet.